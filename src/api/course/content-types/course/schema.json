{
  "kind": "collectionType",
  "collectionName": "courses",
  "info": {
    "singularName": "course",
    "pluralName": "courses",
    "displayName": "Course"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "minLength": 3,
      "maxLength": 255
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true
    },
    "description": {
      "type": "richtext",
      "required": true
    },
    "category": {
      "type": "enumeration",
      "enum": [
        "programming",
        "design",
        "business",
        "marketing",
        "management",
        "soft-skills",
        "other"
      ],
      "required": true
    },
    "level": {
      "type": "enumeration",
      "enum": [
        "beginner",
        "intermediate",
        "advanced"
      ],
      "required": true
    },
    "duration": {
      "type": "integer",
      "required": true,
      "description": "Duration in minutes"
    },
    "instructor": {
      "type": "string",
      "required": true,
      "maxLength": 255
    }
  }
}
